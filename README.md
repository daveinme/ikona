# Ikona

Ikona is a GTK-based application for importing and viewing images.

## Compilation

To compile Ikona, you need to have Meson and a C compiler installed.

```bash
meson build
cd build
ninja
```

## Run

To run the application, execute the following command:

```bash
./ikona
```
